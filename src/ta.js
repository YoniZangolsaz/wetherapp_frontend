// /* // --- Directions
// Given an array of strings with duplicates, return a new array with unique values.
// --- Example
//   dedupe(["a", "b", "b", "c", "d", "a"]) === ["a", "b", "c", "d"]
//  */
//   function dedupe(arr) {
//     const a = { }
//     arr.forEach(element => {
//       a[element] =  " " , 
//     })
//     return Object.keys(a)
    
//   }
//   module.exports = dedupe;


//   * @param {Object} scrollableElement - The scrollable element to check the scroll position of.
// * @param {Array<Object>} elements - An array of objects representing the elements to check the heights of.
// * Each object should have a `height` property indicating its height in pixels.
// *
// * @returns {Number} The index of the currently visible element, or -1 if no element is visible.
// */

// function getVisibleElementIndex(scrollableElement, elements) {
//     const elem = scrollableElement.ofser
//     for (let index = 0; index < elements.length; index++)
//         const elem ===  elements[index];
//     }
// }

// const get = ()

// module = getVisibleElementIndex;

// function isPalindrome(str) {
//     for (let i = 0; index < str.length % 2; index++) {
//         if ( str[i] !== str[str.length - 1 -i] ) {
//             return false
//         }
//     }

//     return true


//     // your code here. Good luck!
//   }

let a = 0 
// console.log(a++);
console.log(++a);

  